export default class Pattern {
  notes = {} // keyed by resolution of beat, for now 16th notes

  addNote(beat, note) {

  }
}

const pattern = new Pattern()
patern.addNote(1, 1)
patern.addNote(4, 1)
patern.addNote(4, 1)
patern.addNote(1, 1)
